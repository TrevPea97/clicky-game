import React from "react";
import "./Jumbotron.css";

const Jumbotron = () => (
	<header className = "header">
		<h1>Clicky Game!</h1>
		<h2>Click on any image to earn a point, but don't click twice.</h2>
	</header>
);
export default Jumbotron;